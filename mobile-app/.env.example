# Expo Environment Variables (Prefix with EXPO_PUBLIC_)
# Set this to override the auto-detected local IP address.
# Useful if running the backend over a tunnel (e.g., ngrok) or connecting to a staging/production server.
# EXPO_PUBLIC_API_URL=https://your-backend-tunnel.ngrok-free.app/api
